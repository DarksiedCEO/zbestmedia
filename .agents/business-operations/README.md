# Z Best Media Business Operations Installation

This local package installs exactly six Operations Grandmasters. The 16 Engineering Grandmasters remain external and separate, making the organizational total 22 while this repository-local registry contains exactly six entries.

Aaliyah remains separate, excluded, and untouched. Five roles are initially active for supervised preparation only. Client Success & Delivery remains inactive until legitimate formal intake or payment is evidenced.

No email, outreach, publishing, scheduling, contacting, messaging, public-state mutation, customer-record creation, production change, merge, or live operation is enabled. All roles must read `authoritative-business-record.json` and `contracts.json` before acting.

This package has no trusted signer and no approval ledger. Handoff `approvalClaim` metadata is untrusted and non-authorizing. Every package-produced handoff must state `authorizationDecision: DO_NOT_EXECUTE`, `verificationStatus: PENDING_HUMAN_VERIFICATION`, and `externalVerificationRequired: true`. Only Andre Love or an out-of-band trusted verifier may issue an authorization receipt outside this package.
